# 模型报告

## 限制

本 Demo 使用规则驱动的模拟数据。模型指标主要验证数据、训练、量子核和预测接口能够端到端运行，不代表真实市场表现。风险得分经过模型校准，但不应解释为真实风险发生概率。

量子核模型使用本地无噪声模拟器和固定规模分层子集。比较结果只说明相同数据与标签下的实验表现，不用于宣称量子模型具备投资收益或风险识别优势。

## 可复现配置

- 默认随机种子：`20260613`
- 特征数/量子比特数：8
- 特征映射：`ZZFeatureMap(reps=2, entanglement=linear)`
- 完整模拟样本数：600
- 默认量子实验规模：180

## 全量实验结果

共享 8 维量子核使用全部 480 条非测试样本训练，并在固定 120 条测试集评估。其 F1 分别为仓位 `0.044`、波动 `0.091`、流动性 `0.000`。全量数据提升了 AUC，但共享核仍过度接近单位阵，默认边界明显偏向负类。

风险专用核调优后，仓位、波动、流动性风险的 F1 分别为 `0.976`、`0.976`、`0.981`。主要改进来自按标签移除无关特征、缩小输入角度、调整核深度与 SVM 参数，并使用仅基于非测试数据交叉验证得到的决策阈值。

该高指标与模拟标签由明确阈值规则生成直接相关，不能外推到真实市场数据，也不能作为量子优势证据。
